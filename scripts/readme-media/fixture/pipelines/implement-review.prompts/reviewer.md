Review the implementer's change on the static web shop.
Write your verdict (`pass` or `fail`) in the frontmatter of your `review` output. Keep the review short: the verdict, then right after it a small left-to-right Mermaid flowchart (`flowchart LR`, three or four boxes of two or three words each) of the flow you checked, then at most three bullets.
Open the page, take screenshots of the change, annotate them with Pillow, and hand them over in `screenshots`.
